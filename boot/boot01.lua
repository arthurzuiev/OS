-- sets global variables and functions
local fs = component.proxy(component.list("filesystem")())

local function load()
    return
    end

    -- todo make component.x dict global
    
end

return {
    load = load
}